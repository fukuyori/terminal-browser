# 変更履歴

[README](README.ja.md) · [English](CHANGELOG.md)

terminal-browser Windowsフォークの主な変更を記載します。

## 0.8.0-win.1

上流のterminal-browser v0.8.0を基にしています。以下は`0.5.8-win.1`以降の変更です。

### 追加

- **ブラウザUI刷新とアプリ連携。** 上流v0.8.0までのブラウザ外枠、タブバー、コンテキストメニュー、
  コマンドのあいまい検索を取り込みました。埋め込み型ターミナルアプリは、通常のブラウザ操作を維持した
  ままテーマ・終了API、アプリタブ、ブラウザセッション連携を利用できます。
- **agent-browser互換操作。** エージェントは`terminal-browser action`を使用して、開いているブラウザへ
  `snapshot`、`click`、`fill`、`eval`を実行できます。ブラウザとタブを指定して複数セッションを操作
  でき、エージェントが操作中のタブにはインジケーターが表示されます。
- **ページ要素のエージェント送信。** `Ctrl+G`または**send to agent**でページ上の要素を選択できます。
  選択内容はクリップボードへコピーされ、同じターミナルタブ内でコーディングエージェントを検出できた
  場合は、その会話へ直接送信されます。
- **WindowsでのSSH対応。** Chromiumと描画処理をローカルで動作させたまま、ネットワーク通信をSSH
  サーバー経由にできます。Windows OpenSSHを使用したリモートバンドルの配置と起動にも対応し、
  Windows固有のプロセス起動、引数処理、終了処理を実装しました。
- **Windows向けエージェントskill setup。** `terminal-browser setup`は同梱skillを、検出したClaude
  Code、Codex、Cursor、Geminiの設定先と共有skillディレクトリへリンクします。対応エージェントは、
  terminal-browserの起動と操作に必要なコマンドリファレンスを参照できるようになります。
- **Windowsリリース自動化。** GitHub ActionsでWindowsペイロード、ポータブルZIP、Inno Setup
  インストーラーをビルドしてテストします。安定版ジョブでは署名証明書を読み込み、公開前にリリース用
  実行ファイルへ署名できます。
- **バージョン付きポータブルアーカイブ。** ZIP名に完全なフォーク版番号を追加しました。たとえば
  `terminal-browser-0.8.0-win.1-windows-x64.zip`となるため、リリースページからダウンロードした後も
  ファイルだけでバージョンを識別できます。

### 変更

- **ターミナルとエージェントの検出。** 対応ターミナルとマルチプレクサーの連携を更新しました。
  terminal-browser自身のペインと近くにあるコーディングエージェントのペインを識別し、選択したページ
  要素を適切な会話へ送りやすくなりました。
- **進捗表示。** オーバーレイを無効にしている場合は進捗表示を描画しないようにしました。
  `--no-overlays`を使用するアプリモードでも、意図しないブラウザUIが表示されません。
- **Windows成果物の命名。** Node.js形式の`win32-x64`を、より明確な`windows-x64`へ変更しました。
  ZIP、マニフェスト、インストーラーのメタデータ、ワークフロー成果物で同じ識別子を使用します。
- **インストーラー作成時の同梱ファイル署名。** `package-windows-inno.ps1 -Sign`はInno Setup実行前に
  ペイロードを確認し、有効な署名がない実行ファイルを署名します。その後、Inno Setupがインストーラーと
  生成されるアンインストーラーを署名します。
- **外部ツールのビルド出力。** 固定バージョンの外部`agent-browser`パッケージに由来するRust警告を
  抑制し、専用インストール先を子Cargoプロセスの`PATH`へ設定しました。terminal-browser自身のRust
  コードに対する警告は引き続き有効です。
- **Windows起動時の出力。** 同梱Node.jsのSQLite `ExperimentalWarning`をWindowsランチャーで抑制
  しました。これにより、terminal-browserを起動するたびに表示されていた警告がなくなります。
  ExperimentalWarning以外のNode.js警告は引き続き表示されます。

### 修正

- **Windowsのマウスホイール座標。** ホイールイベントが古いマウス移動座標ではなく、ホイール報告に
  含まれる座標を使用するようにしました。ボタンとホイール入力は報告するものの、個別の移動イベントを
  送らないターミナルでもスクロールできます。
- **バックグラウンドのコンソールウィンドウ。** デーモンによるターミナルやマルチプレクサーのコマンド
  実行を非表示のWindowsプロセスに変更しました。ブラウザ起動時やペイン検出時に一瞬表示されるコンソール
  ウィンドウを防ぎます。
- **Windows SSHのライフサイクル。** SSHオプション解析、プロセス起動、リモートバンドル起動、終了処理を
  修正しました。Unix固有のプロセス動作を前提にせず、Windowsで利用可能なOpenSSHとアーカイブツールを
  使用します。

## 0.5.8-win.1

上流のterminal-browser v0.5.8を基にした、最初のWindowsフォーク版です。

### 追加

- **Windows x64ネイティブ実行環境。** Rustエンジン、CLI、ブラウザデーモン、ランチャーを移植し、
  互換レイヤーを使用せずPowerShellとWindowsターミナルから実行できるようにしました。
- **Win32 ConsoleとConPTY連携。** Windowsのコンソール環境に対応するキーボード、マウス、サイズ変更、
  ウェイクアップ、コンソールサイズ、ターミナル復元処理を追加しました。
- **Windows IPCとパス処理。** Unix固有だったセッション通信をWindows名前付きパイプへ置き換え、
  Windowsネイティブの実行ファイル、ファイルシステム、クリップボード画像、`file://`パスへ対応しました。
- **Windowsインストーラーとポータブル版。** 英語・日本語UI、ユーザー`PATH`への任意登録、WezTerm
  ショートカット、アンインストールに対応したユーザー単位のInno Setupインストーラーを追加しました。
  実行環境一式を含むポータブルZIPも任意で作成できます。
- **Authenticode署名。** ネイティブ同梱ファイル、インストーラー、生成されるアンインストーラーの署名に
  対応しました。配布元による有効な署名がすでにあるファイルは維持します。
- **Windows描画転送。** WezTerm向けの高速なkittyファイルフレーム転送と、kitty graphicsの応答が
  ConPTYを通過しない環境向けのiTerm2 PNGフォールバックを追加しました。

### 修正

- **サイズ変更時のフレーム。** WezTermが新しいフレームを読み込むまで古いフレームファイルを保持し、
  サイズ変更中に送信済みの画像データが消える問題を修正しました。
- **中断されたセッションの清掃。** 通常の終了処理を実行できず強制終了されたプロセスが残した一時
  フレームファイルを削除する処理を追加しました。
- **ターミナル画面の復元。** 終了時にターミナル画像と画面状態を明示的に消去し、PowerShellプロンプトの
  上に最後のブラウザフレームが残る問題を修正しました。
- **スタートメニューからの起動。** ショートカットを`wezterm-gui.exe`へ変更し、`wezterm.exe`によって
  余分なコンソールウィンドウが表示される問題を修正しました。
- **インストール完了時の動作。** インストール終了時のterminal-browser自動起動を廃止し、切断済み
  クライアントに関する警告やインストーラープロセスへのログ出力を防ぎました。
